# 导出说明模板

这份文件用于说明某个 mod 的导出约定，当前只提供占位结构。

## 当前约定

- `exports/debug/`：调试阶段的临时构建输出。
- `exports/release/`：未来可安装到游戏目录中的候选成品。
- 只有 `release` 目录中的最终文件，才应进入安装流程。

## 接入真实构建流程时需要补充

- 真实的构建命令。
- 导出后需要保留的文件名单。
- 打包命名规则。
- 安装前校验项。

## 注意

- 不要把 `src/`、原始素材、草稿文档直接视为可安装成品。
- 不要在这里记录未经验证的 STS2 API 细节。
